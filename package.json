{
  "name": "smartfaq-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "yarn workspace @smartfaq/web-student lint && yarn workspace @smartfaq/web-admin lint",
    "format": "yarn workspace @smartfaq/web-student format && yarn workspace @smartfaq/web-admin format",
    "format:check": "yarn workspace @smartfaq/web-student format:check && yarn workspace @smartfaq/web-admin format:check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11"
  },
  "lint-staged": {
    "apps/web-student/src/**/*.{ts,tsx}": [
      "yarn --cwd apps/web-student lint --fix --max-warnings=0",
      "prettier --write"
    ],
    "apps/web-admin/src/**/*.{ts,tsx}": [
      "yarn --cwd apps/web-admin lint --fix --max-warnings=0",
      "prettier --write"
    ],
    "apps/api/app/**/*.py": [
      "node scripts/lint-python.cjs"
    ]
  }
}
