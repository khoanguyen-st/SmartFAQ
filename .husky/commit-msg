# Commit message format: type(scope): message
# Scope is optional and can use () or [] without a space.
# Examples:
#   feat(api): add new endpoint
#   fix[web-admin]: resolve styling issue
#   docs: update README

commit_msg=$(cat "$1")

# Skip merge commits
if echo "$commit_msg" | grep -qE "^Merge"; then
  exit 0
fi

# Check format: allow optional scope in () or [] and optional space before it
if ! echo "$commit_msg" | grep -qE "^(feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert)([[:space:]]*(\\(.+\\)|\\[.+\\]))?: .+"; then
  echo "❌ Invalid commit message format!"
  echo ""
  echo "Format: type(scope): message"
  echo ""
  echo "Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build, revert"
  echo "Example: feat(api): add user authentication"
  echo "         fix(web-admin): resolve pagination bug"
  exit 1
fi
